spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/mydata?serverTimezone=UTC
    username: root
    password: root
logging:
  file:
    path: D:/log/
    max-size: 1MB
  level:
    root: info
